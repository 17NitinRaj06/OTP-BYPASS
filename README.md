# OTP-BYPASS
This project demonstrates the process of bypassing One-Time Password (OTP) authentication using Burp Suite. OTP bypass is a critical security issue that, if not properly mitigated, can expose systems to unauthorized access.
